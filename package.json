{
  "private": true,
  "name": "vagrantfiles",
  "scripts": {
    "fmt": "run-p format",
    "format": "prettier --write \"**/*\"",
    "test": "run-p lint",
    "lint:yaml": "prettier -l \"**/*\""
  },
  "devDependencies": {
    "npm-run-all2": "5.0.2",
    "prettier": "3.2.5"
  }
}

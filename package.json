{
  "private": true,
  "name": "vagrantfiles",
  "scripts": {
    "fmt": "run-p format",
    "format": "prettier --write \"**/*\"",
    "test": "run-p lint",
    "lint:yaml": "prettier -l \"**/*\""
  },
  "devDependencies": {
    "npm-run-all2": "5.0.0",
    "prettier": "3.2.5"
  }
}

{
  "private": true,
  "name": "vagrantfiles",
  "scripts": {
    "fmt": "run-p format",
    "format": "prettier --write \"**/*\"",
    "test": "run-p lint",
    "lint:yaml": "prettier -l \"**/*\""
  },
  "devDependencies": {
    "npm-run-all": "4.1.5",
    "prettier": "1.19.1"
  }
}
